import { navLinks } from "../../constants/navigationConstants";

describe("testing the home component rendering", () => {
  it("checks to see if the top navbar is rendered on the screen", () => {});
});
